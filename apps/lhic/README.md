# LHIC npm compatibility entry point

This package makes `npx lhic` run the full
[`@pinyencheng/lhic`](https://www.npmjs.com/package/@pinyencheng/lhic) CLI.

For a persistent global CLI and local Playwright Chromium runtime, run:

```bash
npx @pinyencheng/lhic install cli
```
